{
  "name": "chirimen-drivers",
  "version": "1.0.0",
  "repository": "git@github.com:chirimen-oh/chirimen-drivers.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^7.3.1",
    "lerna": "^3.22.1"
  },
  "scripts": {
    "release": "lerna publish from-package --yes --pre-dist-tag next",
    "lint": "eslint .",
    "lint-report": "eslint --output-file eslint_report.json --format json ."
  }
}
