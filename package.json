{
  "name": "chirimen-drivers",
  "version": "1.0.0",
  "repository": "git@github.com:chirimen-oh/chirimen-drivers.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "~6",
    "lerna": "~3"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "release": "lerna publish",
    "lint": "eslint --ext .mjs packages",
    "lint-report": "eslint --output-file eslint_report.json --format json --ext .mjs packages"
  }
}
